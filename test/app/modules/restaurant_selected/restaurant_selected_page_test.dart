import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/restaurant_selected/restaurant_selected_page.dart';

void main() {
  testWidgets('RestaurantSelectedPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RestaurantSelectedPage(title: 'RestaurantSelected')));
    //  final titleFinder = find.text('RestaurantSelected');
    //  expect(titleFinder, findsOneWidget);
  });
}
