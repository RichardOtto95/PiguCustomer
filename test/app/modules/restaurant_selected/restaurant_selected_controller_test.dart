import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu/app/modules/restaurant_selected/restaurant_selected_controller.dart';
import 'package:pigu/app/modules/restaurant_selected/restaurant_selected_module.dart';

void main() {
  initModule(RestaurantSelectedModule());
  // RestaurantSelectedController restaurantselected;
  //
  setUp(() {
    //     restaurantselected = RestaurantSelectedModule.to.get<RestaurantSelectedController>();
  });

  group('RestaurantSelectedController Test', () {
    //   test("First Test", () {
    //     expect(restaurantselected, isInstanceOf<RestaurantSelectedController>());
    //   });

    //   test("Set Value", () {
    //     expect(restaurantselected.value, equals(0));
    //     restaurantselected.increment();
    //     expect(restaurantselected.value, equals(1));
    //   });
  });
}
