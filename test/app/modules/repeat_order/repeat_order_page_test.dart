import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/repeat_order/repeat_order_page.dart';

void main() {
  testWidgets('RepeatOrderPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RepeatOrderPage(title: 'RepeatOrder')));
    //  final titleFinder = find.text('RepeatOrder');
    //  expect(titleFinder, findsOneWidget);
  });
}
