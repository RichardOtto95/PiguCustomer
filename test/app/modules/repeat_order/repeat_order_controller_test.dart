import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu/app/modules/repeat_order/repeat_order_controller.dart';
import 'package:pigu/app/modules/repeat_order/repeat_order_module.dart';

void main() {
  initModule(RepeatOrderModule());
  // RepeatOrderController repeatorder;
  //
  setUp(() {
    //     repeatorder = RepeatOrderModule.to.get<RepeatOrderController>();
  });

  group('RepeatOrderController Test', () {
    //   test("First Test", () {
    //     expect(repeatorder, isInstanceOf<RepeatOrderController>());
    //   });

    //   test("Set Value", () {
    //     expect(repeatorder.value, equals(0));
    //     repeatorder.increment();
    //     expect(repeatorder.value, equals(1));
    //   });
  });
}
