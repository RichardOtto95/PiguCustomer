import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/table_info/table_info_page.dart';

void main() {
  testWidgets('TableInfoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TableInfoPage(title: 'TableInfo')));
    //  final titleFinder = find.text('TableInfo');
    //  expect(titleFinder, findsOneWidget);
  });
}
