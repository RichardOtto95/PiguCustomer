import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/invite_to_share/invite_to_share_page.dart';

void main() {
  testWidgets('InviteToSharePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InviteToSharePage(title: 'InviteToShare')));
    //  final titleFinder = find.text('InviteToShare');
    //  expect(titleFinder, findsOneWidget);
  });
}
