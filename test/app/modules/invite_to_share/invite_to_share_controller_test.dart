import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu/app/modules/invite_to_share/invite_to_share_controller.dart';
import 'package:pigu/app/modules/invite_to_share/invite_to_share_module.dart';

void main() {
  initModule(InviteToShareModule());
  // InviteToShareController invitetoshare;
  //
  setUp(() {
    //     invitetoshare = InviteToShareModule.to.get<InviteToShareController>();
  });

  group('InviteToShareController Test', () {
    //   test("First Test", () {
    //     expect(invitetoshare, isInstanceOf<InviteToShareController>());
    //   });

    //   test("Set Value", () {
    //     expect(invitetoshare.value, equals(0));
    //     invitetoshare.increment();
    //     expect(invitetoshare.value, equals(1));
    //   });
  });
}
