import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu/app/modules/virtual_queue/virtual_queue_controller.dart';
import 'package:pigu/app/modules/virtual_queue/virtual_queue_module.dart';

void main() {
  initModule(VirtualQueueModule());
  // VirtualQueueController virtualqueue;
  //
  setUp(() {
    //     virtualqueue = VirtualQueueModule.to.get<VirtualQueueController>();
  });

  group('VirtualQueueController Test', () {
    //   test("First Test", () {
    //     expect(virtualqueue, isInstanceOf<VirtualQueueController>());
    //   });

    //   test("Set Value", () {
    //     expect(virtualqueue.value, equals(0));
    //     virtualqueue.increment();
    //     expect(virtualqueue.value, equals(1));
    //   });
  });
}
