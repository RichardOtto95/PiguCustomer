import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/virtual_queue/virtual_queue_page.dart';

void main() {
  testWidgets('VirtualQueuePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VirtualQueuePage(title: 'VirtualQueue')));
    //  final titleFinder = find.text('VirtualQueue');
    //  expect(titleFinder, findsOneWidget);
  });
}
