import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/pay_the_bill/pay_the_bill_page.dart';

void main() {
  testWidgets('PayTheBillPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PayTheBillPage(title: 'PayTheBill')));
    //  final titleFinder = find.text('PayTheBill');
    //  expect(titleFinder, findsOneWidget);
  });
}
