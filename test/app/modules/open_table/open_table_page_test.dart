import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/open_table/open_table_page.dart';

void main() {
  testWidgets('OpenTablePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OpenTablePage(title: 'OpenTable')));
    //  final titleFinder = find.text('OpenTable');
    //  expect(titleFinder, findsOneWidget);
  });
}
