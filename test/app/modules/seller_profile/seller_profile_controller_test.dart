import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  //initModule(SellerProfileModule());
  // SellerProfileController sellerprofile;
  //
  setUp(() {
    //     sellerprofile = SellerProfileModule.to.get<SellerProfileController>();
  });

  group('SellerProfileController Test', () {
    //   test("First Test", () {
    //     expect(sellerprofile, isInstanceOf<SellerProfileController>());
    //   });

    //   test("Set Value", () {
    //     expect(sellerprofile.value, equals(0));
    //     sellerprofile.increment();
    //     expect(sellerprofile.value, equals(1));
    //   });
  });
}
