import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/add_participant/add_participant_page.dart';

void main() {
  testWidgets('AddParticipantPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddParticipantPage(title: 'AddParticipant')));
    //  final titleFinder = find.text('AddParticipant');
    //  expect(titleFinder, findsOneWidget);
  });
}
