import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu/app/modules/add_participant/add_participant_controller.dart';
import 'package:pigu/app/modules/add_participant/add_participant_module.dart';

void main() {
  initModule(AddParticipantModule());
  // AddParticipantController addparticipant;
  //
  setUp(() {
    //     addparticipant = AddParticipantModule.to.get<AddParticipantController>();
  });

  group('AddParticipantController Test', () {
    //   test("First Test", () {
    //     expect(addparticipant, isInstanceOf<AddParticipantController>());
    //   });

    //   test("Set Value", () {
    //     expect(addparticipant.value, equals(0));
    //     addparticipant.increment();
    //     expect(addparticipant.value, equals(1));
    //   });
  });
}
