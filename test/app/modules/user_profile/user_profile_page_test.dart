import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu/app/modules/user_profile/user_profile_page.dart';

void main() {
  testWidgets('UserProfilePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UserProfilePage(title: 'UserProfile')));
    //  final titleFinder = find.text('UserProfile');
    //  expect(titleFinder, findsOneWidget);
  });
}
